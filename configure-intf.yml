---
- name: Configure CSR Routers
  hosts: routers
  connection: local
  collections:
  - cisco.ios
  tasks:
  - name: Configure CSR Interfaces
    ios_l3_interfaces:
      config:
      - name: GigabitEthernet2
      ipv4:
      - address: { ip_addr }


